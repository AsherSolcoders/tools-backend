fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
slowapi==0.1.9
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
Pillow==11.0.0
numpy==2.2.1
pypdf==5.1.0
PyMuPDF==1.25.1
python-docx==1.1.2
qrcode==8.0
python-barcode==0.15.1
# Used directly by the JSON/YAML converter. It already arrived as a transitive
# dependency of uvicorn[standard], but a tool that imports it should say so —
# otherwise a future uvicorn release could drop it and break the tool silently.
PyYAML==6.0.2
# Regex engine with a wall-clock timeout, used to run visitor-supplied patterns
# without a catastrophic backtrack freezing the server (app/core/regex_guard.py).
# The guard degrades to refusing risky patterns if this is not installed.
regex==2024.11.6

# Optional: ML background removal (rembg). Needs ~500 MB RAM for the U2Net model,
# so it is NOT installed by default — the Background Remover automatically uses the
# lightweight color-key engine when these are absent. Install only on a >=2 GB host:
#   pip install rembg==2.0.59 onnxruntime==1.20.1
